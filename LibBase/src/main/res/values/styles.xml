<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 应用统一风格配置 弹窗类 -->
    <!-- 有暗色背景的Dialog -->
    <style name="LoadingDialog" parent="@android:style/Theme.Holo.Light.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <declare-styleable name="LeftIconHeader">
        <attr name="centerTitle"/> <!-- 中间标题 -->
    </declare-styleable>

    <declare-styleable name="LeftIconRightIconHeader">
        <attr name="centerTitle"/> <!-- 中间标题 -->
        <attr name="rightIcon"/> <!-- 右边图标 -->
    </declare-styleable>

    <declare-styleable name="LeftIconRightTextHeader">
        <attr name="centerTitle"/> <!-- 中间标题 -->
        <attr name="rightText"/> <!-- 右边标题 -->
    </declare-styleable>
</resources>